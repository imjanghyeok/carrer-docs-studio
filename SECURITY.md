# 보안·개인정보 경계

## 지원하는 사용 환경

신뢰하는 개인 컴퓨터의 단일 사용자·단일 서버를 전제로 합니다. `127.0.0.1`로만 바인딩합니다. 공용 서버, 외부 포트 포워딩, 터널, 공유 PC의 여러 사용자 서비스로 노출하지 마세요. 별도의 사용자 계정·권한 서버를 구현한 제품이 아닙니다.

## 로컬에 남는 내용

- 기준 HTML, 편집본, 변경 패치, 문서 이름
- AI 요청·답변·수정 전후 HTML 조각과 적용 여부
- 최신 PDF
- 회사명·지원 직무·공고 출처 URL, 가져온 파일 사본과 추출 텍스트, 자료 목록

기본 데이터 위치는 소스 저장소 밖입니다. 별도 암호화 DB를 제공하지 않으므로 OS 계정 권한·디스크 암호화·백업을 관리해야 합니다. 테스트는 임시 폴더에 합성 자료만 생성합니다.

## 외부로 나가는 내용

| 동작 | 네트워크·정보 경계 |
| --- | --- |
| 처음 의존성 설치 | npm 패키지와 Chromium 다운로드 |
| 일반 편집·PDF | 앱 차원의 외부 업로드 없음. 문서 렌더러의 외부 요청 차단 |
| Codex 연결 | Codex 인증 상태·사용 가능한 모델 확인. 문서 본문은 보내지 않음 |
| AI 보내기 | 요청문·선택 HTML·선택한 최근 대화를 AI 제공자에 전송 |
| 회사 자료 가져오기 | 선택한 파일을 개인 데이터 폴더에 복사하고 로컬에서 텍스트 추출. 공고 URL에 접속하지 않음 |
| 회사 자료 피드백 보내기 | 체크한 자료의 텍스트·파일명·종류·회사명·직무·출처 URL. 현재 편집 HTML은 제외. 대화 옵션은 같은 자료 조합에만 적용 |

앱이 분석용 원격 텔레메트리를 수집하지는 않습니다. 외부 CLI·AI 제공자의 인증·진단·보관 정책까지 이 앱이 통제하는 것은 아닙니다. 민감한 자료를 넣기 전 사용 중인 계정과 제공자의 정책을 확인하세요.

CLI의 기존 인증 저장소를 재사용하며 앱은 인증 토큰·설정 원문·CLI stderr를 브라우저로 전달하지 않습니다. 공개 이슈나 스크린샷에 이메일, 토큰, 실제 이력서 문구를 포함하지 마세요.

## 구현한 보호 장치

- HTTP Host 검사와 쓰기/AI 요청의 세션 토큰 검사, 쓰기 요청의 Origin 검사
- 스크립트 실행이 막힌 문서 iframe과 CSP, 로컬 PDF 리소스만 허용
- 정적 리소스의 경로 순회 및 심볼릭 링크 탈출 방지
- 문서 버전·해시 충돌 검사, 검토 후 명시적 AI 수정 적용
- Codex 일회성 작업에 제한된 폴더·읽기 프로필·도구 비활성화 설정 전달
- AI 문자열 치환의 범위·중복·겹침과 신규 능동 콘텐츠·리소스 검사
- 공개 파일 허용 목록, 의심스러운 경로·일부 개인정보/키 패턴 검사

이 장치들이 완전한 보안 검증을 의미하지 않습니다. 악의적인 로컬 프로세스, 계정 탈취, 모든 형태의 HTML/CSS 공격, 사용자가 직접 연 외부 링크까지 방어한다고 주장하지 않습니다. 신뢰하는 템플릿과 로컬 문서를 사용하세요.

## 가져오는 파일

신뢰할 수 있는 로컬 파일을 사용하세요. 8MB 업로드 제한, 확장자·UTF-8/PDF 헤더 검사, 경로 검증과 추출 제한은 백신이나 파일 파서의 완전한 격리를 대신하지 않습니다. Poppler는 최신 보안 업데이트를 적용해 사용하세요. PDF 파서는 별도 OS 샌드박스 없이 사용자 권한으로 실행됩니다.

회사 자료 API는 읽기에도 세션 토큰을 요구합니다. 업로드한 HTML은 실행하지 않고 추출 텍스트를 React 텍스트 노드로 표시합니다. 원본 파일은 정적으로 제공하지 않습니다. 참고자료는 지시가 아닌 데이터로 전달하지만 프롬프트 인젝션을 완전히 제거한다고 보장하지 않습니다. 회사 자료 요청은 질문 전용이며 파일 변경 제안은 강제로 비웁니다.

## 공개 검토

`npm run check:public`은 공개할 텍스트 파일을 `PUBLIC_FILES.json`과 비교합니다. 알려진 계정명 등을 추가로 검사하려면 공개 코드에 쓰지 말고 로컬 `PUBLIC_DENY_TERMS` 환경 변수로 쉼표 구분 문자열을 전달할 수 있습니다. 검사 결과는 파일명·사유만 표시하고 일치한 민감 문자열은 출력하지 않습니다.

검사하지 못하는 개인정보도 있습니다. 전화번호·주소·사진·자유 서술 경력·간접 식별 정보는 수동 검토가 필요합니다. `.test-data`, `dist`, `node_modules`를 배포물로 압축하지 마세요. Git에 강제로 추가된 비허용 파일도 검사하지만, 이미 만들어진 과거 커밋의 모든 내용까지 자동 검사하지 않습니다.

## 문제를 발견했다면

실제 개인정보·인증값이나 악용 가능한 상세 내용을 공개 이슈에 먼저 게시하지 마세요. 향후 원격 저장소가 만들어지면 소유자가 비공개 보안 제보 기능을 설정해야 합니다. 현재는 원격 제보 채널이 없습니다. 재현에는 가상 자료를 사용하고, 유출이 의심되는 키는 해당 제공자의 공식 절차로 폐기·재발급하세요.
